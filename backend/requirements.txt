# =============================================================
#  print3d-shop — backend dependencies
#  Python 3.12+
# =============================================================

# Core framework
django==5.0.6
djangorestframework==3.15.2

# Auth
djangorestframework-simplejwt==5.3.1

# CORS (allow React dev servers to call the API)
django-cors-headers==4.3.1

# Filtering
django-filter==24.2

# Environment variables
python-dotenv==1.0.1

# Image handling (product photos)
Pillow==10.3.0

# Production server (used from Phase 14 onward)
gunicorn==22.0.0

# Static files (used from Phase 14 onward)
whitenoise==6.7.0

# PostgreSQL adapter (used from Phase 14 onward)
psycopg2-binary==2.9.9

# Database URL parser (used from Phase 14 onward)
dj-database-url==2.1.0